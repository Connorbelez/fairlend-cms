# Visual masters only

These renders contain local timing-proxy voice tracks because the available OpenAI API credentials returned `429 insufficient_quota` on 2026-07-17.

Do not distribute these MP4s as final audio masters. Fund or replace the local `OPENAI_API_KEY`, run `scripts/finalize-openai-voice.mjs`, and re-render all six compositions. The source, visual direction, captions, claim language, and on-screen OpenAI TTS disclosure are already prepared for that final pass.
